<h1>Your task list</h1>

<ul>
  <% @tasks.each do |task| %>
    <li>
      <%= link_to task_path(task.id) do %>
        <h2><%= task.name %></h2>
        <p><%= task.details %></p>
      <% end %>
    </li>
  <% end %>
</ul>

<%= link_to "Add a new task", new_task_path %>
